import { OptionChart } from '../interfaces';

export const DEFAULT_OPTIONS: OptionChart = {
  axisX: { valueFormatString: 'D MMM' },
  animationEnabled: true,
  theme: 'light2',
  toolTip: { shared: true },
  data: [],
};
